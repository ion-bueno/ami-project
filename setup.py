from setuptools import setup

setup(
   name='Data Pipeline',
   version='0.1',
   description='Data Pipeline',
   author='group01',
   author_email='foomail@foo.com',
   install_requires=['pandas',
                     'numpy',
                     'matplotlib',
                     'dash'], #external packages as dependencies
)